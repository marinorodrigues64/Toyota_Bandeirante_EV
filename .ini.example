# Copie este arquivo para ".env" (uso local com `vercel dev`)
# ou configure a mesma variável no painel do Vercel em
# Project Settings > Environment Variables.

ANTHROPIC_API_KEY= sk-ant-api03-......
